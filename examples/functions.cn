fun double(x : int) -> int [pure] {
  x + x
}

fun sumPair((a, b) : (int * int)) -> int [pure] {
  a + b
}

main : () [impure] =
  let p = New[int] 0;
  let v = double 21;
  let u = Set[int] (p, v);
  let r = Get[int] p;
  let w = Del[int] p;
  ()
