type option(a) = { Some : a | None : () }

fun toInt(x : option(int)) -> int [pure] {
  case x {
    Some n -> n
  | None u -> 0
  }
}

main =
  (let a = toInt (Some 42 : option(int) [pure]);
   let b = toInt (None () : option(int) [pure]);
   ()) : () [impure]
