fun isAlias((a, b) : (ptr int * ptr int)) -> bool [pure] {
  Eq[ptr int] (a, b)
}

main : () [impure] =
  let p = New[int] 10;
  let q = New[int] 20;
  let same = isAlias (p, p);
  let diff = isAlias (p, q);
  let u = Del[int] p;
  let v = Del[int] q;
  ()
