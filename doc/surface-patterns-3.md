I want to extend the surface language of computation expressions with pattern matching. To do this: 

1. Create a grammar of surface computation expressions and toplevel definitions, as well as toplevel definitions. The analogy is that surface computation expressions bear the same relationship to computation expressions that surface assertions do to core assertions. 

2. Give a set of typing rules for surface computations that elaborates to core computations, again following the pattern of the elaboration rules for surface assertions. 

3. Before implementing anything, first make changes to the Ott, and ensure the Ott definitions compile and do not overflow beyond the end of the page. (Small overflows of the margin are okay.) 

4. Ask the user for clarification for  any cases which do not mirror what happens with the surface elaboration -- do not guess. 

